# Vocalizer
Vocalizer turns a JSON "script" into a single mixed WAV file: synthesized speech (via VoxCPM2), pre-recorded sound-effect clips, silences, and a looping background bed, all stitched into one continuous timeline.
